import { AppPage } from './app.po';
import { Navigation } from 'selenium-webdriver';
import { browser } from 'protractor';

describe('angular-two-recipe-app App', () => {
  let page: AppPage;
  /*
  beforeEach(() => {
    page = new AppPage();
  });

  it('should display title "Recipe App"', () => {
    page.navigateTo();
    expect(page.getTitleText()).toEqual('Recipe App');
  });

  it('should display title "Create Recipe" when clicking the Create Recipe button', () => {
    page.navigateTo();
    page.getCreateRecipeBtn().click();
    //browser.pause();
    expect(page.getTitleText()).toEqual('Create Recipe');
  })
  */
});
